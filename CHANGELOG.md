# 变更记录

## v1.0.0 — 2026-09-24

大五人格 · 野生版（职场人设测试）首个整理发布版本。  
本版本主要消化微信内测反馈，并补上结果页分享、答题交互等问题。

---

### 原始反馈（6 项）及处理

| 编号 | 反馈 | 处理结果 |
|------|------|----------|
| 1 | 选择项跳到下一页依然会保留 | 已修复 |
| 2 | 结果页建议文案还有，需要删除 | 已修复（删「这不是建议，是剧透」） |
| 3 | 首页 30 道题，改成 31 | 已修复 |
| 4 | 有一道题流程慢、易歧义，调整题目 | 已修复（第 27 题） |
| 5 | 右上角分享有问题 | 已优化 |
| 6 | 结果页分享有问题，没有同事对比 | 已按后续反馈改版（见下） |

---

### 问题 1：下一题选项高亮残留

**现象：** 选完一题进入下一题，选项仍像被选中。

**原因：**  
1. 选中态未在切题时清干净；  
2. 手机微信里 `:hover` 会粘在按钮上，看起来像「带着选中」。

**改动：**
- 向前切题不回显已选答案；仅「上一题」才回显
- 整题卡片强制重建，清掉 WebView 粘滞高亮
- 仅在真鼠标设备启用 hover 样式

**涉及文件：** `src/views/Quiz.vue`

---

### 问题 2：结果页「建议 / 剧透」文案

**现象：** 结果正文末尾有「这不是建议，是剧透：xxxx」。

**说明：**  
- 要删的是正文里的**剧透建议**，不是彩蛋条「🥚 这届用户不好带」（彩蛋后另删，见下文）。

**改动：**  
结果页 `storySegments` 不再拼接 `advice` / 「这不是建议，是剧透：」。

**涉及文件：** `src/views/Result.vue`  
（题库里仍保留 `advice` 字段，仅页面不展示）

---

### 问题 3：首页题数 30 → 31

**原因：** 正式题 30 + 不计分彩蛋 1 = 共 31 题。

**改动：** 首页与 `index.html` meta 文案改为 31 道。

**涉及文件：** `src/views/Home.vue`、`index.html`

---

### 问题 4：歧义题目调整

**原题：** 「周会上领导问：这个流程能不能再快点？」易被理解成产品流程本身。

**改为：** 「需求评审卡住了，会议室一片沉默。你会？」并微调选项文案。

**涉及文件：** `src/data/questions.js`（id: 27）

---

### 问题 5 / 6：分享与传播（含后续迭代）

**初版方向：** 结果链接可还原结果、同事对比引导、右上角转发带结果。

**按实测反馈收敛为 v1.0.0 最终形态：**

| 项 | 最终行为 |
|----|----------|
| 按钮文案 | 「📣 复制去发群」 |
| 分享文案 | 称号 + 一句钩子 +「敢不敢来测…」+ 首页链接 |
| 已删除 | 「建议@3个同事…」「先看我的结果…」 |
| 落地页 | 链接进**首页开始测**（旧 `?share=` 结果链也会跳首页） |
| 已删除 UI | 「叫 3 个同事来测」、右上角分享说明文案 |
| 微信标题 | 更新为结果相关 title/description，便于「···」预览 |

**涉及文件：**  
`src/views/Result.vue`、`src/data/questions.js`、`src/utils/shareMeta.js`、`vite.config.js`（`allowedHosts: true`，方便隧道调试）

---

### 本版本追加修复

#### 去掉彩蛋展示文案

**现象：** 「🥚 这届用户不好带」夹在正文与赞赏之间，显得突兀。

**改动：** 结果页不再展示 `result.egg`（彩蛋题仍在答题流中，只是结果页不显示对应句子）。

#### 第 31 题改选后无法再进结果页

**现象：** 从结果页返回第 31 题，再点选项，跳不回结果页。

**原因：** `isFinished === true` 时 `selectOption` 直接 return。

**改动：** 进入答题页 / 再次作答时解除完成锁，最后一题可重新结算并进入结果页。

**涉及文件：** `src/stores/quizStore.js`、`src/views/Quiz.vue`

---

### 本地微信调试说明

微信无法稳定打开局域网 `http://192.x.x.x:5173`，需 HTTPS 隧道（如 Cloudflare Quick Tunnel）。  
开发时 Vite 需 `--host`，并已配置 `server.allowedHosts: true`。

---

### 主要改动文件一览

- `src/views/Quiz.vue`
- `src/views/Result.vue`
- `src/views/Home.vue`
- `src/stores/quizStore.js`
- `src/data/questions.js`
- `src/utils/shareMeta.js`
- `vite.config.js`
- `index.html`
- `package.json`（版本号 → `1.0.0`）
